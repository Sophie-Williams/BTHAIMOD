#ifndef __NORMALIZER_H__
#define __NORMALIZER_H__
/*
#include <vector>

using namespace std;

class Normalizer {

private:
	double _base;
	double _super;

	double _min;
	double _max;

	double _mod;
	double _ratio;

public:
	
	Normalizer(double min, double max);

	Normalizer(double min, double max, double base, double super);

	double normalize(double value);

	void probability(vector<double> * vectors);
};*/

#endif